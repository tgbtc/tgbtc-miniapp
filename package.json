{
  "name": "tgbtc-miniapp",
  "version": "1.0.0-v16-try-prod-metrics",
  "private": true,
  "dependencies": {},
  "scripts": {
    "build": "mkdir -p public && cp index.html public/index.html && cp tonconnect-manifest.json public/tonconnect-manifest.json",
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "description": "tgBTC explorer with Mainnet first, Testnet switch, one Vercel function, and strict prod metrics probing",
  "engines": {
    "node": "20.x"
  }
}
