{
  "name": "tgbtc-miniapp",
  "version": "1.0.0-v15-hobby-one-function",
  "private": true,
  "dependencies": {},
  "scripts": {
    "build": "mkdir -p public && cp index.html public/index.html && cp tonconnect-manifest.json public/tonconnect-manifest.json",
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "description": "tgBTC explorer with Mainnet first, Testnet switch, and one Vercel serverless function for Hobby plan",
  "engines": {
    "node": "20.x"
  }
}
