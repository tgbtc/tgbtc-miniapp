{
  "name": "tgbtc-miniapp",
  "version": "1.0.0-v14-vercel-public-fix",
  "private": true,
  "dependencies": {
    "@noble/secp256k1": "^2.3.0",
    "@ton/core": "^0.63.1"
  },
  "scripts": {
    "build": "mkdir -p public && cp index.html public/index.html && cp tonconnect-manifest.json public/tonconnect-manifest.json",
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "description": "tgBTC full explorer with Mainnet/Testnet switch and metrics proxy",
  "engines": {
    "node": ">=20"
  }
}
